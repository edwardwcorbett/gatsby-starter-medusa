import { IdempotencyKey } from "../models/idempotency-key";
import { RequestContext } from "../types/request";
import { ICartCompletionStrategy, CartCompletionResponse } from "../interfaces";
declare class CartCompletionStrategy implements ICartCompletionStrategy {
    private idempotencyKeyService_;
    private cartService_;
    private orderService_;
    private swapService_;
    constructor({ idempotencyKeyService, cartService, orderService, swapService, }: {
        idempotencyKeyService: any;
        cartService: any;
        orderService: any;
        swapService: any;
    });
    complete(id: string, ikey: IdempotencyKey, context: RequestContext): Promise<CartCompletionResponse>;
}
export default CartCompletionStrategy;
