import { Repository } from "typeorm";
import { MoneyAmount } from "../models/money-amount";
declare type Price = Partial<Pick<MoneyAmount, "currency_code" | "region_id" | "sale_amount" | "currency_code">> & {
    amount: number;
};
export declare class MoneyAmountRepository extends Repository<MoneyAmount> {
    findVariantPricesNotIn(variantId: string, prices: Price[]): Promise<MoneyAmount[]>;
    upsertCurrencyPrice(variantId: string, price: Price): Promise<MoneyAmount>;
}
export {};
